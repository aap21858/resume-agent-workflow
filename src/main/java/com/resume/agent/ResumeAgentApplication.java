package com.resume.agent;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Main Spring Boot application for the Resume Agent Workflow System.
 * This application provides an agentic workflow for optimizing candidate resumes
 * based on client requirements using AI-powered agents.
 */
@SpringBootApplication
public class ResumeAgentApplication {

    public static void main(String[] args) {
        SpringApplication.run(ResumeAgentApplication.class, args);
    }
}
